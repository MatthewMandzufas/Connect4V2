import TestFixture from "./test-fixture";

describe(`test-fixture.js`, () => {
  describe(`Given no parameters`, () => {
    it(`returns a text fixture`, () => {
      const testFixture = new TestFixture();
      expect(testFixture).toBe;
    });
  });
});
